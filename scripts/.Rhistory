tweets <- read.csv(file='../data/realDonaldTrump_tweets.csv', stringsAsFactors = FALSE)
currency <- read.csv(file='../data/Korea_CER.csv', stringsAsFactors = FALSE)
View(tweets)
View(currency)
colnames(currency) <- ['date', 'currency']
colnames(['date', 'currency'])
colnames(currency) <- c('date', 'currency')
View(currency)
currency$date <- as.Date(currency$date)
tweets$created_at <- as.Date(tweets$created_at)
delta1 <- c()
delta2 <- c()
tweets$created_at[1]
library(dplyr)
curr <- currency %>%
filter(date == tweets$created_at[1])
curr
tweets[i]
curr
curr$date-1
for (i in 1:10) {
delta1[i] <- filter(currency, date == tweets$created_at[i]-1)
delta2[i] <- filter(currency, date == tweets$created_at[i]+1)
}
warnings()
delta[i] <- filter(currency, date == tweets$created_at[1])
delta1[i] <- filter(currency, date == tweets$created_at[1])
delta1[i] <- filter(currency, date == tweets$created_at[1])$date
for (i in 1:10) {
delta1[i] <- filter(currency, date == tweets$created_at[i]-1)$date
delta2[i] <- filter(currency, date == tweets$created_at[i]+1)$date
}
for (i in 1:10) {
d <- currency %>%
filter(date == tweets$created_at[i]) %>%
select(date)
# delta1[i] <- filter(currency, date == tweets$created_at[i]-1)$date
# delta2[i] <- filter(currency, date == tweets$created_at[i]+1)$date
}
for (i in 1:10) {
d <- currency %>%
filter(date == tweets$created_at[i]) %>%
select(date)
print(d)
# delta1[i] <- filter(currency, date == tweets$created_at[i]-1)$date
# delta2[i] <- filter(currency, date == tweets$created_at[i]+1)$date
}
for (i in 1:10) {
d <- currency %>%
filter(date == tweets$created_at[i]) %>%
select(date)
curr <- filter(currency, date == d)$date
print(curr)
# delta1[i] <- filter(currency, date == tweets$created_at[i]-1)$date
# delta2[i] <- filter(currency, date == tweets$created_at[i]+1)$date
}
for (i in 1:10) {
d <- currency %>%
filter(date == tweets$created_at[i]) %>%
select(date)
curr <- filter(currency, date == d)$currency
print(curr)
# delta1[i] <- filter(currency, date == tweets$created_at[i]-1)$date
# delta2[i] <- filter(currency, date == tweets$created_at[i]+1)$date
}
for (i in 1:10) {
d <- currency %>%
filter(date == tweets$created_at[i]) %>%
select(date)
curr <- filter(currency, date = d)$currency
print(curr)
# delta1[i] <- filter(currency, date == tweets$created_at[i]-1)$date
# delta2[i] <- filter(currency, date == tweets$created_at[i]+1)$date
}
for (i in 1:10) {
d <- currency %>%
filter(date == tweets$created_at[i]) %>%
select(date)
curr <- filter(currency, date == d)$currency
print(curr)
# delta1[i] <- filter(currency, date == tweets$created_at[i]-1)$date
# delta2[i] <- filter(currency, date == tweets$created_at[i]+1)$date
}
for (i in 1:10) {
#curr <- currency %>%
#     filter(date == tweets$created_at[i]) %>%
#     select(currency)
idx <- match(tweets$created_at[i], currency$date)
#curr <- filter(currency, date == d)$currency
print(idx)
# delta1[i] <- filter(currency, date == tweets$created_at[i]-1)$date
# delta2[i] <- filter(currency, date == tweets$created_at[i]+1)$date
}
for (i in 1:10) {
#curr <- currency %>%
#     filter(date == tweets$created_at[i]) %>%
#     select(currency)
idx <- match(tweets$created_at[i], currency$date)
#curr <- filter(currency, date == d)$currency
print(idx)
before <- currency[idx-1]$currency
current <- currency[idx]$currency
after <- currency[idx+1]$currency
delta1[i] <- current - before
delta2[i] <- after - current
}
currency[319]$currency
currency[319]
currency$currency[319]
for (i in 1:10) {
#curr <- currency %>%
#     filter(date == tweets$created_at[i]) %>%
#     select(currency)
idx <- match(tweets$created_at[i], currency$date)
#curr <- filter(currency, date == d)$currency
print(idx)
before <- currency$currency[idx-1]
current <- currency$currency[idx]
after <- currency$currency[idx+1]
delta1[i] <- current - before
delta2[i] <- after - current
}
for (i in 1:10) {
#curr <- currency %>%
#     filter(date == tweets$created_at[i]) %>%
#     select(currency)
idx <- match(tweets$created_at[i], currency$date)
#curr <- filter(currency, date == d)$currency
print(idx)
before <- currency$currency[idx-1]
current <- currency$currency[idx]
after <- currency$currency[idx+1]
delta1[i] <- (current-before)
delta2[i] <- (after-current)
}
for (i in 1:10) {
#curr <- currency %>%
#     filter(date == tweets$created_at[i]) %>%
#     select(currency)
idx <- match(tweets$created_at[i], currency$date)
#curr <- filter(currency, date == d)$currency
print(idx)
before <- as.numeric(currency$currency[idx-1])
current <- as.numeric(currency$currency[idx])
after <- as.numeric(currency$currency[idx+1])
delta1[i] <- (current-before)
delta2[i] <- (after-current)
}
delta1
delta2
t.test(delta1, delta2, paired=TRUE)
before
current-before
as.numeric(current-before)
unlist(delta1)
unlist(delta1)
unlist(delta2)
t.test(delta1, delta2, paired=TRUE)
delta1
delta1 <- unlist(delta1)
delta2 <- unlist(delta2)
t.test(delta1, delta2, paired=TRUE)
tweets <- read.csv(file='../data/unique_tweets.csv', stringsAsFactors = FALSE)
tweets <- read.csv(file='../data/unique_datess.csv', stringsAsFactors = FALSE)
tweets <- read.csv(file='../data/unique_dates.csv', stringsAsFactors = FALSE)
View(tweets)
tweets <- read.csv(file='../data/unique_dates.csv', stringsAsFactors = FALSE)
View(tweets)
tweets <- read.csv(file='../data/unique_dates.csv', stringsAsFactors = FALSE)
View(tweets)
currency <- read.csv(file='../data/Korea_CER.csv', stringsAsFactors = FALSE)
colnames(currency) <- c('date', 'currency')
currency$date <- as.Date(currency$date)
tweets$created_at <- as.Date(tweets$created_at)
delta1 <- c()
delta2 <- c()
curr <- currency %>%
filter(date == tweets$created_at[1]) %>%
select(date)
for (i in 1:10) {
idx <- match(tweets$created_at[i], currency$date)
print(idx)
before <- as.numeric(currency$currency[idx-1])
current <- as.numeric(currency$currency[idx])
after <- as.numeric(currency$currency[idx+1])
delta1[i] <- (current-before)
delta2[i] <- (after-current)
}
delta1 <- unlist(delta1)
delta2 <- unlist(delta2)
t.test(delta1, delta2, paired=TRUE)
library(dplyr)
tweets <- read.csv(file='../data/unique_dates.csv', stringsAsFactors = FALSE)
currency <- read.csv(file='../data/Korea_CER.csv', stringsAsFactors = FALSE)
colnames(currency) <- c('date', 'currency')
currency$date <- as.Date(currency$date)
tweets$created_at <- as.Date(tweets$created_at)
delta1 <- c()
delta2 <- c()
curr <- currency %>%
filter(date == tweets$created_at[1]) %>%
select(date)
for (i in 1:length(tweets)) {
idx <- match(tweets$created_at[i], currency$date)
print(idx)
before <- as.numeric(currency$currency[idx-1])
current <- as.numeric(currency$currency[idx])
after <- as.numeric(currency$currency[idx+1])
delta1[i] <- (current-before)
delta2[i] <- (after-current)
}
delta1 <- unlist(delta1)
delta2 <- unlist(delta2)
t.test(delta1, delta2, paired=TRUE)
